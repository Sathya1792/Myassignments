let number = 3;
function numtype(number) {
    if (number > 0) {
        return ("positive");
    } else if (number < 0) {
        return ("negative");
    } else {
        return ("neutral");
   
    }
}

console.log(numtype(number));

